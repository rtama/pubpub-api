displayName: Reset Password
description: Used to reset a users password
post:
  description: Reset a users password.
  body:
    application/json:
      formParameters:
        email:
          description: email of the account to login
          required: true
        password:
          description: password after hashing
          required: true
        hash:
          description: You know, I am not sure this is necessary.
  responses:
    200:
      body:
        application/json:
          example: |
            {
            }
    401:
      body:
        application/json:
          example: "Unauthorized"

/reset: !include ../userPasswordReset/userPasswordReset.raml
