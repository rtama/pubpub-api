import app from '../../server';
import { Pub, User, Label, File, Journal, Version, Contributor, FollowsPub, License, InvitedReviewer, Reaction, Role } from '../../models';


// export function getFile(req, res, next) {
// 	// How do we authenticate?
// 	// Then just grab and return
// }
// app.get('/pub/file', getFile);

// export function postFile(req, res, next) {
// 	// Create the file
// 	// Attach to a specific version (update version?)
// 	// Attach version to pub
// 	// return new version
// }
// app.post('/pub/file', postFile);

// export function putFile(req, res, next) {∞
// 	// Update the file, 
// 	// create new version
// 	// attach version to pub
// 	// return new version
// }
// app.put('/pub/file', putFile);

// export function deleteFile(req, res, next) {
// 	// set file inactive
// 	// create new version with file removed
// }
// app.delete('/pub/file', deleteFile);
